name: Keep Render Awake

on:
  schedule:
    - cron: "2/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend
        run: curl -fsS https://autolux-a857.onrender.com/health || exit 1
